import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'English-5-Word',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
